let {a} = {atan2(65.01, cos(to_float(847)))}
fn b({ {  } }, c[d] : {}[], e[f] : int[]) : int {
  let g[h] = args
  let i[j] = args
  let k[l] = i
  return f
  return (d * f)
}

show b({{{}}}{0}, [{}], {[(78 * 779)]}{0})

let {c[d]} = {args}
let {{e}} = {{tan(sin(atan(a)))}}
fn f(g : {}, { h : int }, { i : float[] }) : bool {
  return false
  let j = [((d + d) != h)]
  let k[l] = i
  let m[n] = i
  return (! true)
}

let {} = {{{}}}{0}{0}
fn g() : float {
  let h[i] = c
  let {} = {}
  let j[k] = {{args}}{0}{0}
  let l[m] = [432]
  return sqrt(acos(tan(asin(pow(81.49, 39.36)))))
}

let h = (- tan(atan(cos(sin(g())))))
let i[j] = args
let k[l] = i
let {m} = {argnum}
