let a[b] = [asin(atan({7.43}{0}))]
let c[d] = args
fn e({  }) : float {
  let f = sin((- tan({1.26}{0})))
  let g[h] = {{c}}{0}{0}
  let {{i[j]}} = {{{g}}}{0}
  let k = [(to_int(to_float(487)) == 576)]
  return atan(cos({18.13}{0}))
}

read image "f.png" to f

read image "g.png" to g

let h[i] = a
let {} = {}
let j = ((! (d <= d)) == {(to_float(985) <= to_float(i))}{0})
fn k() : int {
  let {} = {{}}{0}
  let l = sin(72.95)
  let {} = {{{}}}{0}{0}
  return argnum
  return (- argnum)
}

let l[m] = [h]
fn n({  }) : int[] {
  let o[p] = args
  let {} = {}
  let q = cos(32.79)
  let {} = {}
  return [{{585}}{0}{0}]
}

let o[p] = h
