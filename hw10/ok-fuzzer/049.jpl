let a = (asin(to_float(argnum)) / cos(to_float(argnum)))
let b = acos(46.65)
time let c[d] = args

let {e[f]} = {c}
show log(acos(pow(exp(cos(51.0)), to_float((argnum + f)))))

let g[h] = args
let i = args
type j = int
fn k() : float[] {
  let {l[m]} = {{{g}}}{0}{0}
  let n[o] = [atan2((- acos(atan(a))), sin(b))]
  let p = sqrt({{b}}{0}{0})
  let q = l
  return [{{b}{0}}{0}]
}

read image "l.png" to l

read image "m.png" to m

let n = 649
