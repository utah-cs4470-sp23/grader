let a = acos(atan(tan(pow(to_float(273), 35.3))))
fn b({ { c[d] : float[] } }, { e : {float} }) : int {
  let f[g] = c
  let {{}} = {{{{}}}}{0}{0}
  return g
  let h[i] = [g]
  return i
}

let c[d] = [to_float(argnum)]
show argnum

let {} = {{}}{0}
fn e() : bool {
  let {} = {}
  let f = [tan(atan2(6.15, a))]
  let {{g[h]}} = {{{c}}{0}}
  let i[j] = c
  return (h == h)
}

let f[g] = [a]
let h[i] = c
let {{}} = {{}}
read image "j.png" to j

let {k[l]} = {{[j]}{0}}
let m[n] = f
