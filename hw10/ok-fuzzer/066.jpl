show [(! (argnum > {argnum}{0}))]

let a = to_int(35.34)
fn b(c[d] : int[], e : bool[], f[g] : {}[]) : int {
  let {} = {{}}{0}
  let {h} = {{{{}}}{0}}{0}
  let {i[j]} = {args}
  let k[l] = i
  return ({{a}}{0}{0} * g)
}

show cos((to_float(argnum) / {pow(69.85, 19.74)}{0}))

let c = args
let {} = {{{}}}{0}{0}
fn d(e[f] : int[], { g[h] : int[] }) : int {
  let i[j] = c
  let {} = {}
  let {{k}} = {{cos((- 9.12))}}
  let {} = {}
  return (a - j)
}

let e[f] = c
let g = [c]
fn h(i : {bool}, j : int) : bool {
  return (true != false)
  let k[l] = args
  let {} = {}
  let m[n] = e
  return i{0}
}

fn i(j[k] : float[]) : {int} {
  return {{{a}}}{0}{0}
  let {} = {{{}}}{0}{0}
  let {} = {{{}}}{0}{0}
  let l = to_float(f)
  return {(- (a + (- 539)))}
}

let j[k] = args
