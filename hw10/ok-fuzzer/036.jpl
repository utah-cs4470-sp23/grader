let a = (396 / argnum)
let b = atan2({pow(sqrt(10.93), to_float(63))}{0}, asin(cos({58.82}{0})))
let c[d] = [a]
fn e() : bool {
  let f = to_float(d)
  let g[h] = c
  let i[j] = {{c}}{0}{0}
  let k = f
  return {(! (a <= to_int(k)))}{0}
}

let f = atan2(sin(to_float(a)), {atan2((- b), (- b))}{0})
let g = atan(74.73)
let h = exp(to_float(704))
let i[j] = {[a]}{0}
let {k} = {cos(exp(b))}
let l[m] = i
let n = to_int(sin(b))
let {} = {}
