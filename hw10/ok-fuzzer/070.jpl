let {a} = {pow(to_float(477), atan2(asin(exp(5.97)), (- 94.95)))}
let b[c] = [false]
fn d() : bool[] {
  let e = (- pow(atan(atan2(exp(a), a)), atan(a)))
  let f = [727]
  let {} = {{{}}}{0}{0}
  return [(true == (39.71 >= sqrt(tan(50.51))))]
  return {{[false]}}{0}{0}
}

fn e({ {  } }, f[g] : bool[], { h[i] : int[] }) : bool {
  let {{j[k]}} = {{{h}}}{0}
  let l = atan(exp(tan(acos(log(a)))))
  let {m[n]} = {{{j}}{0}}{0}
  let o = atan(sqrt(log(tan(to_float(i)))))
  return (to_int(({a}{0} / atan((- a)))) < (- {n}{0}))
}

let {} = {}
let f = (- {(- 776)}{0})
fn g({  }, h : int) : int {
  let {i} = {(- log(a))}
  let {} = {}
  let {} = {}
  let j = exp(to_float({(- h)}{0}))
  return 715
}

let h = e({{{}}}{0}, d(), {{args}}{0})
let i = to_int(sqrt(sin(sqrt(exp(73.98)))))
let {j[k]} = {d()}
read image "l.png" to l

let m[n] = [{a}{0}]
