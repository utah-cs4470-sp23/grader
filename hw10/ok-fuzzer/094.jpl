read image "a.png" to a

fn b() : int {
  let c = exp(log(asin(cos(94.44))))
  let d = sin(atan(atan2(78.23, atan2(cos(c), c))))
  let {} = {{{}}}{0}{0}
  let e[f] = {{[{}]}}{0}{0}
  return {{339}}{0}{0}
}

print "c"

let {{}} = {{{{}}}{0}}{0}
let c[d] = args
assert (0.86 != to_float(d)), "e"
read image "e.png" to e

let f[g] = args
read image "h.png" to h

let i = (true == (! false))
time let j = args

let k[l] = c
