fn a(b : bool[], c : {}) : bool {
  let {{d[e]}} = {{{[argnum]}}}{0}
  let {} = c
  return {{(argnum < e)}}{0}{0}
  let f = acos(atan2(exp(sin(exp(17.96))), (- cos(exp(34.02)))))
  return {(! {true}{0})}{0}
}

let b = ({{2.99}}{0}{0} >= acos(acos(asin(tan(85.89)))))
fn c({ d : bool }, e[f] : {}[]) : bool {
  let {} = {{{}}}{0}{0}
  let g[h] = {e}{0}
  let i = pow(asin(cos(cos(asin(1.96)))), atan(log(log((- 80.44)))))
  let j = acos(atan(cos(exp(to_float(f)))))
  return (({i}{0} % log((asin(j) - j))) != exp(sqrt(atan2(40.87, j))))
}

let {} = {{{}}{0}}{0}
let d[e] = [to_float((((- 716) + argnum) - 975))]
let {f} = {atan2((sqrt((- 45.3)) + (- to_float(264))), {to_float(argnum)}{0})}
let g[h] = d
let {i[j]} = {g}
fn k({  }, l : {}, m[n] : bool[]) : bool {
  return false
  let o = true
  return b
  let p[q] = {args}{0}
  return {o}{0}
}

fn l() : bool {
  let m[n] = args
  let {o[p]} = {{{i}}}{0}{0}
  let q = j
  let {} = {{{}}}{0}{0}
  return {{(j == j)}}{0}{0}
}

let m[n] = {{d}}{0}{0}
let o[p] = {{[m]}}{0}{0}
