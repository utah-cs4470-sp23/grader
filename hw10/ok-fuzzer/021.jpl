let a[b] = args
let {c[d]} = {[to_float(to_int(to_float(argnum)))]}
read image "e.png" to e

read image "f.png" to f

time let g = exp(exp(sin(74.23)))

read image "h.png" to h

fn i({  }) : float {
  let j[k] = c
  assert false, "l"
  let l[m] = [false]
  let n = f
  return atan2(sqrt(asin({38.41}{0})), log(cos(tan(g))))
}

let {} = {{{}}}{0}{0}
let j[k] = {[g]}{0}
let l = exp(atan2(g, g))
let m[n] = args
write image e to "o.png"

