let {} = {{{}}}{0}{0}
let a[b] = {{[60.75]}}{0}{0}
show sqrt((exp((- to_float(argnum))) + tan(to_float(to_int(63.05)))))

let c[d] = a
fn e(f[g] : bool[], h : bool) : {bool} {
  let i = a
  let j[k] = a
  let l = (- (((- (- argnum)) - (d % k)) * d))
  let m[n] = i
  return {true}
}

let f = a
let g[h] = f
fn i() : {float} {
  return {{(- 4.81)}}{0}
  let j[k] = c
  let l[m] = a
  let n = f
  return {to_float(k)}
}

let j[k] = g
let l[m] = c
let {n} = {i()}{0}
let o[p] = j
