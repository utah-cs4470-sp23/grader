time assert (522 >= (30 / (- 441))), "a"

print "a"

print "a"

let a = to_int(sqrt(tan(33.32)))
let b = cos(sqrt(sqrt(sin(tan(72.31)))))
let c = exp(acos(exp(sqrt(b))))
let d = a
read image "e.png" to e

let f = 713
let {{g[h]}} = {{{args}}}{0}
let {} = {}
let {} = {{{}}}{0}{0}
