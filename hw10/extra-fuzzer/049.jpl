let a = atan(78.68)
let b = (to_int((asin(a) / (to_float(78) + sqrt(9.89)))) != ({to_int(a)}{0} * to_int(pow(exp(a), to_float(argnum)))))
let {c, d[e]} = {{13.69, args, {b, args}}{2}, atan2(17.45, pow(to_float(argnum), exp(a)))}{0}
let f[g] = [argnum]
let h = tan(to_float(to_int(log(to_float(g)))))
read image "i.png" to i

let j[k] = d
let l = acos(h)
let m = pow(sin(pow((acos(h) % atan(a)), {a}{0})), h)
let n[o] = {{f}}{0}{0}
show tan(acos(asin(h)))

let p = tan(m)
