let a[b] = [815]
let c[d] = {{a}}{0}{0}
let e = to_int(sqrt((sin(to_float(d)) - log(to_float(argnum)))))
let f[g] = c
let h = pow((cos((- sqrt(18.34))) - 81.33), atan((- ((92.25 - 94.35) / (- 36.37)))))
let i = 28.7
let j = exp(atan2(sin(pow(to_float(896), cos(91.63))), 29.14))
let k = {sin(acos(pow(21.88, i))), pow(sqrt(23.52), h)}{1}
let l[m] = {{j, args, f}}{0}{2}
read image "n.png" to n

read image "o.png" to o

let p[q, r] = {b, o}{1}
