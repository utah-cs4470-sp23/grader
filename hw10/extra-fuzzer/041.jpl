show (682 * 395)

let {} = {{pow(60.42, 24.8), sin(59.36), {}}{2}, (323 >= (to_int(79.91) / 351))}{0}
read image "a.png" to a

read image "b.png" to b

let c[d, e] = a
let f = b
let {g[h, i], j} = {argnum, log(6.42), {{b, a}, (- e)}{0}}{2}
let k = b
let l = log(tan(exp(49.14)))
let {} = {}
let {} = {}
let {} = {{(d != d)}{0}, {}}{1}
