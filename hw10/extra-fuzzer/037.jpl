let a = (atan2(((- sin(42.32)) - log(to_float(argnum))), log(atan(sqrt(6.95)))) + exp(exp(pow(tan(97.68), cos(48.45)))))
let b = argnum
let c = sqrt(pow(a, pow({args, a}{1}, cos(log(4.7)))))
let d = atan2(c, asin(sqrt(a)))
show b

let e = (pow(atan((- d)), log(pow(log(d), atan(a)))) % acos(to_float(b)))
let f = 38
let g = 82.24
let h = atan(sqrt(exp(sqrt(acos(a)))))
let i = f
let j = g
let {} = {{{}, to_float(b), (argnum / 244)}{0}}{0}
