show log(atan2(sin(log(70.58)), to_float(((argnum * argnum) % (argnum % argnum)))))

show ((! (! (22 <= 356))) == (! {(argnum <= argnum), 316}{0}))

let {a} = {to_float(396), {to_float(argnum), {true}}}{1}{1}
let b = [{(299 <= argnum), {a}}{1}{0}]
read image "c.png" to c

let d[e, f] = {sin(56.01), {c}}{1}{0}
let g = [atan2(log(sin(to_float(974))), to_float(to_int(to_float(e))))]
show (- f)

let {} = {}
let h = argnum
let i = (h < argnum)
let {} = {{{}}{0}}{0}
