show atan(exp((sqrt((- 76.13)) + atan2(35.32, to_float(argnum)))))

read image "a.png" to a[b, c]

let {{}, {{d[e], {f[g, h]}}, i[j, k]}} = {{}, {{{args, b, args}{0}, {a}}, a}}
let l = to_int((log(pow(to_float(131), exp(73.24))) - sin(30.89)))
let m = sqrt(asin(9.39))
show args

let n = b
let o = k
read image "p.png" to p

read image "q.png" to q[r, s]

let t = e
let u[v, w] = f
