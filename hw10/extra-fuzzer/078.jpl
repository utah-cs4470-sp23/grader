show sin({to_float(480)}{0})

read image "a.png" to a[b, c]

read image "d.png" to d[e, f]

let {g} = {sin(asin(cos((- 20.07))))}
let h = sqrt((atan2(acos(acos(g)), sqrt(3.85)) * pow(asin(sin(14.85)), to_float(e))))
let i = (argnum > argnum)
let j[k] = {64.37, {false, args}}{1}{1}
read image "l.png" to l

let m = (! (log(asin(exp(83.54))) == to_float(argnum)))
let n = k
show 59.97

let o = d
