let a = (to_float((- (- (894 / argnum)))) - asin(cos(tan(92.21))))
let b = a
show sqrt(a)

let {} = {{b, acos(b)}, exp(sin(10.85)), {(- 895), acos(a), {}}}{2}{2}
show a

read image "c.png" to c

let {d} = {{{argnum}}}{0}{0}
let e = cos(sqrt(b))
let f = sin(exp(pow((e % atan(e)), atan(e))))
let {{g[h, i]}} = {{{c}}, (- b), pow(atan2(a, atan2(14.81, a)), {e}{0})}{0}
let j[k] = {exp(log(e)), {i, args, sqrt(70.98)}{1}}{1}
let l[m] = {args, f, j}{2}
