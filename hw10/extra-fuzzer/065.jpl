let a[b] = [74.23]
let {c} = {{{b}}{0}{0}}
show tan(exp(atan2(asin(to_float(271)), atan((66.0 / 43.64)))))

let {} = {}
show cos(sqrt(exp(sqrt((38.9 - 1.57)))))

let {} = {}
let {} = {}
read image "d.png" to d

read image "e.png" to e

let f = e
let {g, h} = {{d, {a, 645}}, log(to_float(argnum))}{0}{1}
let {i} = {argnum}
